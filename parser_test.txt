int sub(int z, int q, int r) {
    int g[100];
    int h;
    int i;
    if ( x > y )
        return(z+z);
    else
        x = 5;
}

void main(void) {
    int y;
    int z;
    y = sub(x);
    while ( x + 3 * y > 5 ) {
        x = y + m / z;
        m = x - y + z * m / z;
    }
    z = 8;
}

//void main(void) {
//	a = yeetsub(4); // should throw an error since yeetsub() is undeclared. Also should throw an error if parameters and arguments do not match!
//	sub(4, 6);
//	intmainvoid(a = 4 + 4, b, c, d); // error because undeclared function
//	newsub(); // error because undeclared function
//	return;
//}

